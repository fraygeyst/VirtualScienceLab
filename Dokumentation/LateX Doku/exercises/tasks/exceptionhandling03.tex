In dieser Aufgabe sollen Sie eine eigene Ausnahme Klasse definieren und anwenden.
Diese soll dieselbe Funktionalität aus der vorherigen Aufgabe aufweisen.
Verwenden Sie dazu die \lstinline$raise$-Anweisung um die Ausnahme auszulösen.

