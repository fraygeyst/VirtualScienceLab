% !TeX root = ./pythonTutorial.tex
\begin{enumerate}
\item Erweitern Sie das Tuple aus der vorangegangenen Aufgabe um zwei weitere Elemente. 
Verwenden Sie hierfür die \lstinline$list$- und \lstinline$tuple$-Konstruktoren.
\begin{lstlisting}[language=Python]
# Hinweis

tuple1 = (1, 2, 3)
tuple2 = tuple((1, 2, 3))
tuple3 = tuple(tuple1)
\end{lstlisting}

\item Verwenden Sie die zuvor genutzten Konstruktoren und die \lstinline$str()$-Methode, 
um den Datentyp der Elemente des folgenden Tuples zu String abzuändern und geben Sie das
Tuple zur Validierung auf der Konsole aus:
\\\lstinline$stringTuple = ("hallo", 1, 2.1, False, "string")$
\end{enumerate}