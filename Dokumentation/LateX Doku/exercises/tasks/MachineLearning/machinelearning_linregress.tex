\begin{enumerate}
	\item Erstelle ein Skript, welches eine lineare Regression aus dem \lstinline$wine$-Datensatz erzeugt. Dafür kann \lstinline$datasets.loads_wine$ aus der \lstinline$sklearn$-Bibliothek benutzt werden. 
	\item Mit Spalte 3 und 5 erhält man die Abhängigkeit zwischen dem Alkohol- und dem Magnesiumgehalt. 
	\item Stelle die Datensätze und die Linie für die Regression in einem Scatterplot dar. Hierfür empfiehlt sich für x- und y-Achse der Maximalwert von 25. 
	\item Gebe dem Plot außerdem einen Titel und Achsenbeschriftungen.
\end{enumerate}