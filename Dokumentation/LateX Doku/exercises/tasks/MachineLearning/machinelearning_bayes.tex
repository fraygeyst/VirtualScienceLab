\begin{enumerate}
	\item Lege eine neue Python-Datei an.
	\item Lade über \lstinline$sklearn$ den Datensatz \lstinline$wine$.
	\item Lasse die \lstinline$target_names\lstinline$ und einen Teil der Daten anzeigen, um einen Überblick darüber zu bekommen.
	\item Erzeuge aus den Daten den X-Wert, aus den \lstinline$target_names$ die Y-Werte jeweils als \lstinline$numpy-array$.
	\item Erzeuge eine Instanz des \lstinline$sklearn.naive_bayes.GausssianNB$.
	\item Führe mittels \lstinline$train_test_split$ eine Aufteilung der Daten durch.
	\item Führe eine Vorhersage durch und lasse das Ergebnis anzeigen.
\end{enumerate}