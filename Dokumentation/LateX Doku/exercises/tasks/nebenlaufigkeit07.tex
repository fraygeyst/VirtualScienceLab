\label{nebenlaufigkeit07}
Passen Sie die \hyperref[threads:lst:parkhaus]{\lstinline$CarPark$-Klasse} so an, dass nun eine passende
\lstinline$Semaphore$ verwendet wird.
Übernehmen Sie die \hyperref[threads:lst:auto]{\lstinline$Car$-Klasse} und den
\hyperref[threads:lst:parkhaus_ausführung]{Code zur Ausführung}.
Führen Sie Ihr Programm aus und überprüfen Sie, dass es korrekt funktioniert.

\tip{
Um den aktuellen Zählerwert der \lstinline$Semaphore$ zu erhalten kann das Attribut \lstinline$\_value$
verwendet werden.
Dieses sollte allerding immer nur für Debug-Zwecke eingesetzt werden!
}