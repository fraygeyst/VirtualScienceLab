Damit eine Liste unbhängig von der Anzahl der Elemente durchlaufen wird, wird am besten eine \lstinline$for$-Schleife verwendet. \\
Der Zähler wird durch eine zusätzliche Variable realisiert, welche bei jedem Durchlauf der Schleife um eins inkrementiert wird.

\lstinputlisting{exercises/src/iterator/iterator01.py}