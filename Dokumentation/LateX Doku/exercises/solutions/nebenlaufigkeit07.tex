Die Attribute \lstinline$cv$, \lstinline$capacity$ und \lstinline$occupied$ können entfernt werden.
Im Konstruktor wird nun nur noch ein neues \lstinline$BoundedSemaphore$-Objekt angelegt und
der Parameter \lstinline$capacity$ übergeben.
Die Methode \lstinline$is_free()$ gibt nun den Wert des Ausdrucks \lstinline$0 < self.semaphore._value$
zurück.
In \lstinline$show()$ wird \lstinline$self.semaphore._value$ ebenfalls verwendet, um die Ausgabe
anzupassen.
Die Methoden \lstinline$enter()$ und \lstinline$exit()$ rufen nun entsprechend die beiden Methoden
\lstinline$acquire()$ und \lstinline$release()$ der \lstinline$Semaphore$ auf.

\lstinputlisting[language=Python,linerange={1-2,8-25}]{exercises/src/nebenlaufigkeit07.py}
